{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm install && cd server && npm install && npx prisma generate && cd ../client && npm install && cd .. && npm run build"
  },
  "deploy": {
    "preDeployCommand": "cd server && npx prisma db push && npx prisma db seed",
    "startCommand": "cd server && npm run start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 120
  }
}
