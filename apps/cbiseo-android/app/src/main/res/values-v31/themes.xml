<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- API 31+ — Theme.SplashScreen 속성 (원형 런처 아이콘 기본 스플래시 차단) -->
    <style name="Theme.CbiseoApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_window_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_blank</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="android:windowBackground">@drawable/splash_window_layer</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="postSplashScreenTheme">@style/Theme.CbiseoApp.SplashContent</item>
    </style>
</resources>
