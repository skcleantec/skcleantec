# BRIEF — 공지 HTML 버튼·링크·FAQ

완성 HTML을 넣으면 등록 후에도 버튼, 목차 이동, FAQ 접기, 표·칸 색이 보입니다.

- CSS 변수(`:root`)를 공지 칸에 붙여 버튼·칩 색이 살아납니다.
- `#발주서` 같은 목차, `/notice/…`·외부 링크, `<details>` FAQ가 동작합니다.
- 검색용 JSON-LD는 남기고, 화면을 바꾸는 일반 스크립트만 저장 시 뺍니다. 캡처 자리 숨김은 앱이 대신 합니다.
- 상대 경로 사진(`images/…`)은 툴바 「사진」으로 다시 올려야 합니다.
