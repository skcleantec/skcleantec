# CodeGuardian — 이용 순서 상세

- `workflowGuideSteps.ts`에 `sections` 추가. 막대 JSX는 `StaffWorkflowGuideDetail`로 분리.
- 서버·Prisma 없음. `scrollSection` 동작 유지.
- `npx tsc -b --noEmit` 통과.
- 도움말 소스 + `build-help-data.mjs` 반영.
