# CRM(텔레CRM) 점검 — Maestro 전문 리포트

**Agents:** CodeGuardian, DesignPulse, RoleQA, PlatformOps, DbSentinel  
**일시:** 2026-09-01 12:35 KST

---

## 1. 범위

- PC: `/admin/crm`, `/admin/crm/settings/*`, 숨고 동반 창
- Server: `/api/crm/*` (`server/src/modules/telecrm/`)
- Android: `apps/telecrm-android` (Play `com.cbiseo.marketer`)
- 연동: 숨고 브릿지, WS dispatch/incoming-ring

---

## 2. CodeGuardian

### 구조 (양호)

- 서버 **42파일** — routes / service / helpers 분리
- 클라 **`components/crm/` 55파일**, hooks·api·shared 타입 분리
- `requireFeature('mod_telecrm')` → `requireTelecrmUserAccess` → `requireStaffPermission('crm.view'|'crm.settings')`

### 타입 검사

- `client` tsc — ✅
- `server` tsc — ✅

### 이슈

| Sev | 항목 |
|-----|------|
| MEDIUM | `CrmPage.tsx` **1993 lines** — `.cursor/rules/client-page-modularization.mdc` 위반 |
| MEDIUM | CRUD routes: `findFirst({ id, tenantId })` 후 `update({ where: { id } })` — multitenant red-flag pattern (현재는 선행 검증으로 안전) |
| LOW | 이중 `authMiddleware` on parent + child routers |
| LOW | `mobile-incoming-ring`, `mobile-dispatch/pending` — `crm.view` 생략 (Android 의도로 문서화됨) |

### 테스트

- **TeleCRM dedicated tests: NONE**
- `server/package.json` — no `"test"` script for CRM
- `client` — no test runner
- Android — no unit/instrumentation tests found
- `verify:multitenant:*` — telecrm-specific coverage 없음

---

## 3. DesignPulse

- CRM 설정·워크데스크: slate 카드, `components/crm/layout` 패턴
- 팝업 `/admin/crm` — AdminLayout 밖 전용 UX
- **CrmPage** 단일 파일 과대 → UI 변경 시 회귀·리뷰 부담
- Android WebView CRM 아님 — native + API (OK)

---

## 4. RoleQA — 시나리오 매트릭스 (정적)

| # | 시나리오 | 기대 |
|---|----------|------|
| 1 | mod_telecrm OFF 테넌트 | CRM 메뉴·API 403 |
| 2 | crm.view only | 워크데스크 OK, settings 일부 제한 |
| 3 | 전화 lookup | 동일 tenant inquiry만 |
| 4 | PC → 앱 dispatch | 큐 → 앱 tel:/smsto: |
| 5 | 수신 ring | WS → PC auto lookup |
| 6 | 숨고 AI | requireTelecrmPlatform('soomgo') |
| 7 | 타 tenant id API | 403/404 |

**미실행:** live browser/app — 사용자 수동 확인 권장.

---

## 5. PlatformOps

- `mod_telecrm` in `shared/tenantFeatureModules.ts`
- Platform tenant panel: TeleCRM license/seats
- Railway `TELECRM_APP_*` — Play manifest (릴리스 규칙 준수)

---

## 6. DbSentinel

- Customer lookup: phone/name → **tenantId + operatingCompany** scoped
- Call sessions linked to inquiry with tenant check
- PIPA: phone stored in inquiry/CRM — tenant isolation critical; no cross-tenant leak pattern found in lookup service
- **sms-log API 미구현** — 발송 이력 서버 저장은 Phase 3+ (문서)

---

## 7. 결론

| 질문 | 답 |
|------|-----|
| 깔끔하게 잘 만들어졌나? | **서버·컴포넌트 분리는 우수**, **CrmPage 거대 파일은 예외** |
| 테스트 문제 없나? | **자동 테스트 부재** → 「전혀 문제 없음」 **불가**; 수동·운영 검증 의존 |
| 배포 블로커? | **없음** (tsc OK, 게이트 OK) |

---

## 8. 권장 액션 (우선순위)

1. P1 — 운영 전 **수동 스모크** 체크리스트 1회 (staging)
2. P2 — `telecrmCustomerLookup.service` + `telecrmCallSession.service` **unit tests** 5~10 cases
3. P3 — `CrmPage.tsx` 분할 리팩터 (별도 PR)
